year=2020:2031;
year_str=num2cell(year);

for i=1:length(year_str)
    data=import('./data/dose'+year_str{i});
    if i==1
        al_mm=data(:,1);
        TID_year=data(:,end);
    else
        TID_year=[TID_year,data(:,end)];
    end    
end
